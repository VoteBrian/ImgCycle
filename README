#ImgCycle

ImgCycle is a simple demo app that uses an ImageView as the background, then cycles through a list of images at a set interval.

Place background image in the drawables folder, then add them to the list under in res/values/arrays.xml

